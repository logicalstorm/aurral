---
title: slskd
description: Soulseek-backed downloads for flows and playlists.
---

slskd handles Soulseek acquisition for flow and playlist tracks. Aurral can also use [Usenet](/integrations/usenet/) via Prowlarr and SABnzbd/NZBGet as an alternate or fallback source.

![Aurral history with slskd downloads](../../../assets/screenshots/history.webp)

## Setup

1. If Lidarr already uses slskd on a shared `/data` mount, give Aurral that same mount ([Match Lidarr](/getting-started/storage/)).
2. Connect slskd in onboarding or **Settings → Download Clients → slskd**.

Aurral moves completed files from slskd into your Downloads Folder path.

If slskd runs on Windows and reports host paths like `N:\...`, mount the parent folder into Aurral and add a path mapping under **Settings → Download Clients → Remote Path Mappings**.

## Worker settings

Open the **slskd** card in **Settings → Download Clients** to configure:

- **Source priority**: order relative to Usenet (default `10`)
- **Preferred format** and **Strict format only**: ranking for search results
- **Clean up after runs**: clear completed slskd jobs when a flow or playlist run finishes

## Download fallback

When Usenet or yt-dlp is also configured, Aurral tries sources in priority order. If slskd fails, Aurral can fall back automatically. See [Usenet](/integrations/usenet/#download-source-priority-and-fallback) and [yt-dlp](/integrations/ytdlp/).
