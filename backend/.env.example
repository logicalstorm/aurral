# ============================================
# Environment Variables (All Optional)
# ============================================
# Most configuration can be done via the web UI in Settings.
# These env vars are only needed if you want to override UI settings
# or configure before first launch.
#
# For Docker: Only map your music directory to /data
#   docker run -v /your/music:/data ...
# Everything else can be configured via the web UI.
# ============================================

# Port (optional, defaults to 3001)
# PORT=3001

# slskd Configuration (can be set via UI in Settings > Integrations)
# SLSKD_URL=http://localhost:5000
# SLSKD_API_KEY=your_slskd_api_key

# Slskd Download Path (optional - auto-detected from slskd API if not set)
# Only needed if slskd API doesn't return the correct path
# SLSKD_DOWNLOAD_DIR: Base download directory (app will append /complete)
# SLSKD_COMPLETE_DIR: Direct path to completed downloads folder
# SLSKD_REMOTE_PATH_MAPPING: Map remote paths to local paths (for Docker/remote setups)
#   Format: remote_path:local_path (e.g., /mnt/downloads:/data/downloads)

# Music Library Root Path (optional, defaults to /data)
# For Docker: Use volume mounts instead: -v /your/path:/data
# MUSIC_ROOT=/data
# or
# DATA_PATH=/data

# MusicBrainz e-mail (can be set via UI in Settings > Integrations)
# Technically optional but highly recommended
# CONTACT_EMAIL=user@example.com

# LastFM API Key (can be set via UI in Settings > Integrations)
# Optional but speeds up the app significantly
# LASTFM_API_KEY=your_lastfm_api_key_optional
