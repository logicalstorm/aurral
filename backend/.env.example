# General Config (must complete)
PORT=3001
SLSKD_URL=http://localhost:5000
SLSKD_API_KEY=your_slskd_api_key

# Slskd Download Path Configuration
# SLSKD_DOWNLOAD_DIR: Base download directory (slskd will use this + /complete for finished downloads)
# SLSKD_COMPLETE_DIR: Direct path to completed downloads folder (overrides SLSKD_DOWNLOAD_DIR)
# SLSKD_REMOTE_PATH_MAPPING: Map remote paths to local paths (for Docker/remote setups)
#   Format: remote_path:local_path (e.g., /mnt/downloads:/data/downloads)
# SLSKD_DOWNLOAD_DIR=/path/to/slskd/downloads
# SLSKD_COMPLETE_DIR=/path/to/slskd/downloads/complete
# SLSKD_REMOTE_PATH_MAPPING=/mnt/downloads:/data/downloads

# Music Library Root Path
# Default: /data (for Docker - remap via volume mounts: /your/path:/data)
# For local development, set to a path you can write to (e.g., ~/music or ./data)
# MUSIC_ROOT=/Users/yourname/music
# or
# DATA_PATH=/Users/yourname/music

# MusicBrainz e-mail
# Technically optional but highly recommended
CONTACT_EMAIL=user@example.com

# LastFM API Key
# Optional but speeds up the app a lot
LASTFM_API_KEY=your_lastfm_api_key_optional
