# ============================================
# Required Environment Variables
# ============================================

# Port (defaults to 3001 if not set)
PORT=3001

# Music Library Root Path (defaults to /data)
# For Docker: Map your music directory to /data
#   -v /your/music:/data
MUSIC_ROOT=/data

# Slskd Complete Downloads Directory (defaults to /downloads)
# For Docker: Map slskd's complete directory to /downloads
#   -v /your/slskd/downloads/complete:/downloads
SLSKD_COMPLETE_DIR=/downloads
